{
  "name": "@gryphon/provider-config",
  "version": "1.5.2",
  "description": "Provider configuration for Gryphon: model catalogs, pricing tables, dropdown options, per-provider extra-args filter. Pure data + selection layer (cc-switch lesson). No spawning, no streaming, no UI.",
  "private": true,
  "license": "MIT",
  "author": "POLLEO.AI",
  "main": "./src/index.js",
  "files": [
    "src/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
